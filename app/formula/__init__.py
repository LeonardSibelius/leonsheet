"""Formula language: tokenizer, parser, evaluator.

Hand-written recursive-descent — using a parser library would defeat the
demonstration. See app/formula/tokenizer.py for the grammar this layer
serves; the full grammar lives in the v1.0 spec.
"""
